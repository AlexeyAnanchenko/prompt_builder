# Prompt Builder — Обзор проекта

## 📌 Суть проекта
**Prompt Builder** — это веб-приложение на Streamlit, предназначенное для построения, версионирования и безопасной обработки промптов для Large Language Models (LLM). Основная цель — автоматизировать подготовку контекстно-насыщенных промптов с маскированием конфиденциальных данных (например, названий таблиц, полей, параметров, SQL-формул, JSON-конфигураций) и обеспечить обратную расшифровку ответов LLM.

Ключевые возможности:
- **Версионирование системных промптов** — сохранение, загрузка и управление разными версиями системных промптов.
- **Контекстный поиск** — извлечение релевантной схемы БД (PostgreSQL) для заданного namespace.
- **Маскирование данных** — автоматическая замена реальных имён объектов на абстрактные маски (например, `person` → `ENT_1`) с поддержкой сложных паттернов (SQL-функции, JSON, Java-условия).
- **Расшифровка ответов** — восстановление оригинальных имён из замаскированных ответов LLM.
- **Токенизация** — подсчёт токенов в промптах с использованием быстрого Rust-токенизатора (Hugging Face `tokenizers`).
- **Интерактивный интерфейс** — трёхшаговый процесс с переключаемыми секциями, вкладками и визуальными настройками.

## 🗂️ Структура файлов

### Корневые файлы
- **`app.py`** — точка входа, инициализирует Streamlit-приложение, настраивает страницу и рендерит три основных шага.
- **`requirements.txt`** — зависимости Python (Streamlit 1.51.0, psycopg2-binary, tokenizers, python-dotenv и др.).
- **`.python-version`** — указание версии Python (3.12.10).
- **`README.md`** — документация по установке, использованию и архитектуре.
- **`LICENSE`** — лицензия Apache 2.0.
- **`.gitignore`** — шаблоны игнорируемых файлов (логи, виртуальные окружения, данные версий).
- **`prompt_versions.json`** — файл хранения версий системных промптов (создаётся автоматически).

### Каталоги и модули

#### `config/`
- **`settings.py`** — централизованные настройки: конфигурация страницы, лимиты токенов, текстовые сообщения, параметры БД.

#### `core/` (ядро логики)
- **`context_engine.py`** — содержит три основных класса:
  - `DbDataLoader` — загрузка и индексация данных БД.
  - `ContextResolver` — разрешение контекста (граф сущностей) на основе выбранных datasets/entities.
  - `OutputGenerator` — генерация SQL-вставок с поддержкой маскирования.
- **`masking.py`** — класс `ContextMasker` для регистрации и замены реальных имён на маски и обратно. Поддерживает паттерны: dictGet, литералы, свойства через точку, параметры в фигурных скобках, Java-переменные. Реализует методы `mask_text`, `unmask_text`, `mask_json`, `mask_formula`.
- **`prompt_generator.py`** — сборка финального промпта из системного промпта, контекста SQL и пользовательского запроса.
- **`version_manager.py`** — управление версиями системных промптов (сохранение/загрузка в JSON-файл).
- **`schema_config.py`** — константы первичных ключей и маппинга полей для маскирования (PRIMARY_KEYS, FIELD_MAPPING).

#### `services/`
- **`database.py`** — менеджер подключения к PostgreSQL, пул соединений, загрузка данных по namespace.
- **`context_service.py`** — сервис для оркестрации подбора контекста и генерации промптов. Содержит методы `pick_context` и `generate_final_prompts`.

#### `ui/` (пользовательский интерфейс)
- **`components.py`** — переиспользуемые компоненты Streamlit: анимированный заголовок, кнопки, счётчик токенов, превью версий.
- **`styles.py`** — кастомные CSS-стили для улучшения визуального оформления.
- **`pages/`** — три шага приложения:
  - `step1_system_prompt.py` — настройка и управление версиями системного промпта.
  - `step2_context.py` — загрузка контекста из БД, выбор datasets/entities, генерация промпта с маскированием.
  - `step3_chat.py` — чат-транслятор для шифрования/расшифровки диалога с LLM.

#### `utils/` (вспомогательные утилиты)
- **`logger.py`** — настройка логгирования в файл и консоль.
- **`session.py`** — инициализация и управление состоянием Streamlit session_state.
- **`tokenizer.py`** — подсчёт токенов через библиотеку `tokenizers` (Rust). Использует файл `deepseek_tokenizer/tokenizer.json` (если присутствует), иначе fallback на приближённый подсчёт.
- **`helpers.py`** — вспомогательные функции (копирование в буфер, обрезка строк).

## 🏗️ Архитектура и потоки данных

### Общая схема работы
```mermaid
graph TD
    A[Пользователь] --> B[Шаг 1: Системный промпт]
    B --> C[Шаг 2: Контекст и генерация]
    C --> D[Шаг 3: Чат-транслятор]
    
    subgraph "Backend логика"
        E[DatabaseManager] --> F[Загрузка данных БД]
        F --> G[DbDataLoader]
        G --> H[ContextResolver]
        H --> I[OutputGenerator]
        I --> J[ContextMasker]
        J --> K[PromptGenerator]
        K --> L[Итоговый промпт]
    end
    
    C --> E
    L --> C
    J --> D
```

### Детализация шагов

#### 1. **Шаг 1 — Системный промпт**
- Пользователь вводит или загружает системный промпт.
- Версии сохраняются в `prompt_versions.json` через `VersionManager`.
- Интерфейс: текстовое поле + панель управления версиями (сохранить/загрузить/удалить).
- Реализована отложенная загрузка версий через флаг `_pending_version_load`.
- Обеспечена синхронизация между виджетами и хранилищем данных.
- Управление состоянием осуществляется через `session_state`.

#### 2. **Шаг 2 — Контекст и генерация**
- Выбор namespace из БД → загрузка схемы (таблицы, сущности, параметры) через `DatabaseManager`.
- Выбор datasets/entities для включения в контекст с персистентным хранением выбора.
- Функция **"Подобрать контекст"**: автоматически анализирует граф зависимостей, генерирует SQL-вставки и создаёт словарь масок без генерации финального промпта. Использует `ContextService.pick_context`.
- Генерация SQL-вставок, отражающих выбранный контекст, через `OutputGenerator` с поддержкой маскирования.
- Маскирование имён через `ContextMasker` (реальные → маски) с применением к SQL, формулам, JSON, Java-условиям.
- Сборка финального промпта: системный промпт + SQL-контекст + пользовательский запрос через `PromptGenerator`.
- Подсчёт токенов через `TokenCounter`, отображение замаскированного и оригинального вариантов во вкладках.
- Интерактивный словарь замен: отображается в виде таблицы с категориями, реальными именами и масками.

#### 3. **Шаг 3 — Чат-транслятор**
- Двустороннее преобразование: реальный текст ↔ замаскированный текст.
- Использует тот же словарь замен, что и на шаге 2.
- Позволяет безопасно отправлять замаскированные запросы в LLM и расшифровывать ответы.
- Реализованы режимы редактирования и просмотра (Markdown).
- Настраиваемая высота текстовых областей и баланс колонок.
- Визуальные настройки доступны через expander.
- Кнопки шифрования/расшифровки с мгновенным обновлением.

### Маскирование данных
- **Категории масок**: `ENT` (сущность), `P` (свойство), `PARAM` (параметр), `DS` (датасет), `TBL` (таблица), `DB.DICT` (словарь БД), `COL` (колонка), `OBJ` (прочий объект), `PATH` (путь), `AGG` (агрегация), `LIM` (лимит), `ORD` (сортировка) и др.
- **Формат масок**: `{префикс}_{номер}` (например, `ENT_1`, `P_2`, `PARAM_3`).
- **Регистрация**: при первом обнаружении реального имени генерируется уникальная маска. Реализована защита от двойного маскирования: значения, уже похожие на маски, не регистрируются повторно.
- **Замена**: в тексте промпта и SQL-контексте реальные имена заменяются на маски с учётом границ слов. Поддерживаются сложные конструкции:
  - SQL-функции `dictGet('schema.dict', tuple('col'))` → `dictGet('DB.DICT_1', tuple('COL_2'))`
  - Свойства через точку `Entity.Property` → `ENT_1.P_2`
  - Параметры в фигурных скобках `{param}` → `{PARAM_4}`
  - Java-условия `structRoots != null` → `PARAM_5 != null` (автоматическое определение параметров без фигурных скобок)
  - JSON-конфигурации с полями `entity`, `property`, `parameter` (маскируются рекурсивно)
  - Литералы в кавычках `'value'` (с фильтрацией зарезервированных слов)
- **Расшифровка**: метод `unmask_text` восстанавливает оригинальные имена из замаскированного текста.
- **Словарь**: хранится в `session_state` для согласованности между шагами, отображается в виде интерактивной таблицы с сортировкой по категориям.

### Работа с БД
- **Подключение**: PostgreSQL, пул соединений через `psycopg2`, параметры из переменных окружения (`.env`).
- **Схема**: база `qe_config`, таблицы `namespaces`, `entities`, `parameters`, `datasets`, `tables`, `vertices`, `edges` и др.
- **Загрузка контекста**: фильтрация по `namespace_id`, загрузка связанных таблиц, дополнение глобальными таблицами (`tenants`).
- **Оптимизация**: использование `RealDictCursor` для удобства работы с результатами запросов.

### Токенизация
- Используется библиотека `tokenizers` (Rust) для быстрого подсчёта токенов.
- Файл токенизатора: `deepseek_tokenizer/tokenizer.json` (опционально, если файл отсутствует, используется fallback).
- Fallback: упрощённый подсчёт по словам (длина текста / 0.75), если токенизатор недоступен.
- Отображение счётчика токенов с индикацией превышения лимита (128 000 токенов) через компонент `render_token_counter`.

## 🛠️ Технологический стек
- **Frontend**: Streamlit 1.51.0
- **Backend**: Python 3.12.10
- **База данных**: PostgreSQL (через psycopg2-binary)
- **Токенизация**: Hugging Face tokenizers (Rust)
- **Конфигурация**: python-dotenv
- **Логгирование**: стандартный модуль logging с ротацией в файл
- **Стили**: кастомный CSS, встроенный в Streamlit компоненты
- **Дополнительные библиотеки**: streamlit-clipboard (копирование в буфер), tenacity (повторные попытки), watchdog (отслеживание файлов), pandas (обработка данных), numpy (математические операции) и другие.

## 🚀 Текущее состояние проекта
Проект находится в стадии активной разработки с полностью рабочей базовой функциональностью. Все три шага реализованы и интегрированы. Код сопровождается подробным логгированием и обработкой ошибок. Интерфейс оптимизирован для удобства пользователя, включает визуальные улучшения и интерактивные элементы.

Реализованные функции:
- Управление версиями системных промптов с сохранением в JSON-файл через `VersionManager`.
- Загрузка контекста из PostgreSQL с фильтрацией по namespace через `DatabaseManager`.
- Автоматическое маскирование конфиденциальных данных с поддержкой сложных паттернов (SQL-функции, JSON, Java-условия) через `ContextMasker`.
- Генерация финальных промптов с разделением на замаскированный и оригинальный варианты через `PromptGenerator` и `ContextService`.
- Интерактивный словарь замен с отображением в виде таблицы с сортировкой по категориям.
- Чат-транслятор для двустороннего преобразования текста (шифрование/расшифровка) с поддержкой редактирования и просмотра Markdown.
- Настраиваемый интерфейс с регулировкой высоты окон и баланса колонок.
- Функция "Подобрать контекст" для автоматического анализа графа зависимостей и предварительного создания словаря масок.
- Подсчёт токенов с fallback-механизмом при отсутствии файла токенизатора.

## ✅ Итог
Проект представляет собой законченное инструментальное решение для инженеров промптов, работающих с конфиденциальными схемами данных. Он сочетает удобный интерфейс, мощную логику маскирования и гибкое управление версиями, что позволяет безопасно и эффективно готовить промпты для LLM.

Все файлы хорошо структурированы, код сопровождается логгированием и обработкой ошибок, архитектура модульная и легко расширяемая.

---

*Документ создан автоматически на основе анализа кодовой базы. Последнее обновление: 2025-12-14.*