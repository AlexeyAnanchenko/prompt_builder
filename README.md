# Prompt Builder 🔨

Приложение для построения промптов с версионированием, маскированием конфиденциальных данных и обратной расшифровкой ответов LLM.

## 📋 Описание

Prompt Builder — это веб-приложение на Streamlit, предназначенное для безопасной подготовки промптов для Large Language Models (LLM). Оно позволяет инженерам промптов работать с конфиденциальными схемами баз данных, автоматически маскируя реальные имена объектов и обеспечивая обратную расшифровку ответов LLM.

**Ключевые возможности:**

- 📚 **Версионирование системных промптов** — сохранение, загрузка и управление разными версиями системных промптов в JSON-файле.
- 🔍 **Контекстный поиск** — загрузка схемы БД PostgreSQL по выбранному namespace, извлечение релевантных таблиц, сущностей и параметров.
- 🎭 **Маскирование данных** — автоматическая замена реальных имён (таблиц, полей, параметров, SQL-функций, JSON-конфигураций) на абстрактные маски (например, `ENT_1`, `P_2`, `PARAM_3`) с поддержкой сложных паттернов.
- 🔓 **Расшифровка ответов** — восстановление оригинальных имён из замаскированных ответов LLM с использованием того же словаря замен.
- 🧮 **Токенизация** — подсчёт токенов в промптах с использованием быстрого Rust-токенизатора (Hugging Face `tokenizers`).
- 💬 **Чат-транслятор** — двустороннее преобразование реального текста ↔ замаскированный текст для безопасного диалога с LLM.
- 🖥️ **Трёхшаговый интерфейс** — интуитивный пошаговый процесс: системный промпт → контекст и генерация → чат-транслятор.

## 🚀 Быстрый старт

### Предварительные требования

- Python 3.12.10 (указан в [`.python-version`](.python-version:1))
- pip (менеджер пакетов Python)
- PostgreSQL (для загрузки контекста) — опционально, можно работать без БД

### Установка и запуск

1. **Клонируйте репозиторий** (если нужно)
```bash
git clone git@github.com:AlexeyAnanchenko/prompt_builder.git
cd prompt_builder
```

2. **Установите зависимости**
```bash
pip install -r requirements.txt
```

3. **Настройте переменные окружения** (опционально)
   - Создайте файл `.env` в корне проекта и укажите параметры подключения к БД (см. `config/settings.py`).
   - Пример переменных: `DB_HOST`, `DB_PORT`, `DB_USER`, `DB_PASSWORD`, `DB_NAME`.
   - Если БД не используется, приложение будет работать с ограниченным функционалом.

4. **Запустите приложение**
```bash
streamlit run app.py
```

5. **Откройте браузер**
   - Приложение автоматически откроется по адресу: `http://localhost:8501`
   - Если не открылось автоматически, перейдите по ссылке из терминала

## 📁 Структура проекта

```
prompt_builder/
├── app.py                 # Основное приложение Streamlit
├── requirements.txt       # Зависимости Python
├── .python-version       # Версия Python (3.12.10)
├── LICENSE               # Лицензия Apache 2.0
├── .gitignore           # Игнорируемые файлы
├── prompt_versions.json  # Автоматически создаваемый файл версий промптов
├── config/               # Конфигурация приложения
│   ├── __init__.py
│   └── settings.py      # Настройки страницы, лимиты токенов, сообщения
├── core/                 # Ядро логики
│   ├── __init__.py
│   ├── context_engine.py # Загрузка данных БД, разрешение контекста, генерация SQL
│   ├── masking.py        # Маскирование и расшифровка имён
│   ├── prompt_generator.py # Сборка финального промпта
│   └── version_manager.py # Управление версиями системных промптов
├── services/             # Сервисы
│   ├── __init__.py
│   └── database.py      # Менеджер подключения к PostgreSQL
├── ui/                   # Пользовательский интерфейс
│   ├── __init__.py
│   ├── components.py    # Переиспользуемые компоненты Streamlit
│   ├── styles.py        # Кастомные CSS-стили
│   └── pages/           # Шаги приложения
│       ├── __init__.py
│       ├── step1_system_prompt.py  # Шаг 1: системный промпт
│       ├── step2_context.py        # Шаг 2: контекст и генерация
│       └── step3_chat.py           # Шаг 3: чат-транслятор
├── utils/                # Вспомогательные утилиты
│   ├── __init__.py
│   ├── helpers.py       # Вспомогательные функции
│   ├── logger.py        # Настройка логгирования
│   ├── session.py       # Управление состоянием Streamlit session_state
│   └── tokenizer.py     # Подсчёт токенов через Hugging Face tokenizers
└── deepseek_tokenizer/  # Внешний ресурс токенизатора
    └── tokenizer.json   # Конфигурация токенизатора для модели DeepSeek
```

## 🛠️ Технологии

- **Frontend**: [Streamlit](https://streamlit.io/) 1.51.0
- **Backend**: Python 3.12.10
- **База данных**: PostgreSQL (через psycopg2-binary)
- **Токенизация**: Hugging Face tokenizers (Rust)
- **Конфигурация**: python-dotenv
- **Логгирование**: стандартный модуль logging с ротацией в файл
- **Стили**: кастомный CSS, встроенный в Streamlit компоненты

## 📖 Как использовать

### Шаг 1: Системный промпт
- Введите или отредактируйте системный промпт в текстовом поле.
- Сохраните текущую версию под уникальным именем.
- Загрузите ранее сохранённую версию из выпадающего списка.
- Управляйте версиями (удаление, переименование) через интерфейс.

### Шаг 2: Контекст и генерация
1. **Выбор namespace** — выберите namespace из базы данных для загрузки схемы.
2. **Загрузка контекста** — нажмите «📥 Загрузить контекст», чтобы получить список datasets, entities, parameters.
3. **Подбор контекста** — используйте «🔍 Подобрать контекст» для автоматического анализа графа зависимостей и генерации SQL-вставок. После подбора появится словарь замен.
4. **Маскирование** — включите переключатель «Маскировать конфиденциальные данные» для замены реальных имён на маски (работает автоматически при генерации).
5. **Генерация промпта** — введите пользовательский запрос и нажмите «🚀 Сгенерировать промпт».
6. **Результат** — получите замаскированный и оригинальный варианты промпта во вкладках, подсчёт токенов, словарь замен в виде таблицы с категориями (ENT, P, PARAM и др.).

### Шаг 3: Чат-транслятор
- **Режим шифрования** — введите реальный текст, нажмите «🔒 Замаскировать», получите замаскированную версию.
- **Режим расшифровки** — вставьте замаскированный ответ LLM, нажмите «🔓 Расшифровать», восстановите оригинальные имена.
- **Настройки интерфейса** — регулируйте высоту текстовых областей, баланс колонок, режим просмотра (Markdown/сырой текст).
- **Словарь замен** — отображается текущий словарь масок, используемый для преобразования.

## ⚙️ Конфигурация

### Файлы данных
- **prompt_versions.json** — автоматически создаётся для хранения версий системных промптов. Добавлен в `.gitignore` для безопасности.
- **.env** — файл с переменными окружения для подключения к БД (пример: `DB_HOST`, `DB_PORT`, `DB_USER`, `DB_PASSWORD`, `DB_NAME`).

### Настройки приложения
Основные параметры заданы в `config/settings.py`:
- Лимиты токенов (MAX_TOKENS = 128000)
- Высоты текстовых областей
- Текстовые сообщения и уведомления
- Конфигурация страницы Streamlit

### База данных
Приложение использует PostgreSQL для загрузки контекста. По умолчанию используется база `query_db` (настраивается через `DB_NAME`). Схема включает таблицы `namespaces`, `entities`, `parameters`, `datasets`, `tables`, `vertices`, `edges` и др. Подключение настраивается через переменные окружения.

## 🤝 Разработка

Для внесения изменений:

1. Установите зависимости: `pip install -r requirements.txt`
2. Запустите в режиме разработки: `streamlit run app.py`
3. Внесите изменения и проверьте их в реальном времени
4. Логи приложения пишутся в `logs/app.log` (каталог создаётся автоматически)

### Тестирование
- Убедитесь, что все модули импортируются без ошибок.
- Проверьте работу маскирования на различных паттернах (SQL-функции, свойства через точку, параметры в фигурных скобках, Java-условия).
- Убедитесь, что токенизация работает корректно (используется fallback, если файл токенизатора недоступен).

### Расширение функциональности
- Добавление поддержки других СУБД через абстракцию DatabaseManager.
- Интеграция с внешними LLM-API для автоматической отправки промптов.
- Расширенная визуализация графа зависимостей сущностей.
- Режим промпт-шаблонов с параметризацией.

## 📄 Лицензия

Проект распространяется под лицензией Apache 2.0. Подробности см. в файле [LICENSE](LICENSE).